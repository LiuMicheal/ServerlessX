{
  "schema": "serverlessx.profile.v1",
  "id": "rdma_dmabuf",
  "title": "RDMA with CUDA DMA-BUF",
  "system": "spd",
  "tier": "portable",
  "maturity": "source-preview",
  "priority": 30,
  "available": false,
  "unavailable_reason": "Native source and build rules are included, but the public hardware deployment and evidence workflow is not yet available.",
  "description": "Builds the direct CUDA VMM DMA-BUF data plane for an RDMA-capable fabric.",
  "requirements": {
    "python_min": "3.9",
    "commands": [
      "c++",
      "make",
      "nvidia-smi"
    ],
    "paths": [
      "/sys/class/infiniband",
      "/usr/local/cuda/include/cuda.h"
    ]
  },
  "capabilities": [
    "cuda-dmabuf",
    "rdma-data-plane"
  ],
  "workflow": [
    "Compile the direct-only shared library and hardware probes with make spd-gdr",
    "Run no transfer until an explicit deployment profile and evidence verifier are available"
  ],
  "claims": [
    "native source and build rules are included",
    "a successful build is not hardware execution evidence"
  ],
  "privileged_operations": []
}
